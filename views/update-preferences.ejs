<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Preferences</title>
</head>
<body>
    <div class="container">
        <h1>Update Preferences</h1>
        <form action="/preferences/update" method="POST">
            <input type="hidden" name="user_id" value="<%= user.user_id %>">

            <div class="form-group">
                <label for="preferred_types">Preferred Workout Types:</label>
                <input type="text" id="preferred_types" name="preferred_types" value="<%= preferences.preferred_types %>" required>
            </div>

            <div class="form-group">
                <label for="preferred_intensity">Preferred Intensity:</label>
                <input type="text" id="preferred_intensity" name="preferred_intensity" value="<%= preferences.preferred_intensity %>" required>
            </div>

            <div class="form-group">
                <label for="preferred_duration">Preferred Duration (in minutes):</label>
                <input type="number" id="preferred_duration" name="preferred_duration" value="<%= preferences.preferred_duration %>" required>
            </div>

            <div class="form-group">
                <label for="preferred_exercise">Preferred Exercises:</label>
                <input type="text" id="preferred_exercise" name="preferred_exercise" value="<%= preferences.preferred_exercise %>" required>
            </div>

            <button type="submit">Update Preferences</button>
        </form>

        <% if (error) { %>
            <p style="color: red;"><%= error %></p>
        <% } %>

        <% if (message) { %>
            <p style="color: green;"><%= message %></p>
        <% } %>
    </div>
</body>
</html>
